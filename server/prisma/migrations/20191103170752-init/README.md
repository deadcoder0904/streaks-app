# Migration `20191103170752-init`

This migration has been generated by Akshay Kadam at 11/3/2019, 5:07:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Habit" (
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "streak" INTEGER NOT NULL DEFAULT 0  ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "lift"."Habit.name" ON "Habit"("name")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191103170752-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,14 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+model Habit {
+  id     String @default(cuid()) @id
+  name   String @unique
+  streak Int
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191103170752-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191103170752-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
